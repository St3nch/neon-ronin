# fixtures/internal-research

Fixtures derived from Internal Research workspace documentation.

Current authorized fixture use:

```text
hand-authored minimal fixture extracted from docs/workspaces/internal-research/workspace-config.draft.md
```

The markdown workspace config is not a database import source.

Fixtures here must not contain provider payloads, credentials, customer data, SearchClarity-specific fields, unbounded metadata, custom_data, executable agents, or integration references.