# apps/desktop

Future desktop app shell.

This folder is reserved for a later Tauri/React desktop app.

Current status:

```text
deferred
```

Do not add Tauri config, frontend code, UI routes, commands, or app packaging here until a separate decision authorizes UI/desktop-shell work.

The first persistence proof must remain in `packages/neon-core` and must not depend on this folder.